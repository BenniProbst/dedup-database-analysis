# COMDARE Cluster - Lab Credentials Template
# Kopiere diese Datei nach .credentials.env und fuelle die Werte aus:
#   cp credentials.env.example .credentials.env
#
# .credentials.env ist in .gitignore geschuetzt und wird NICHT committed.
# Format: KEY=VALUE (kein Leerzeichen um '=', keine Quotes noetig)
# Parser: source .credentials.env oder envsubst in k8s/base/secrets.yaml

# Lab User Passwort (identisch fuer alle DBs)
DEDUP_LAB_PASSWORD=

# Einzelne DB-Passwoerter (normalerweise identisch zum Lab-Passwort)
DEDUP_PG_PASSWORD=
DEDUP_CRDB_PASSWORD=
DEDUP_REDIS_PASSWORD=
DEDUP_KAFKA_PASSWORD=
DEDUP_MARIADB_PASSWORD=
DEDUP_CLICKHOUSE_PASSWORD=

# MinIO S3 (LDAP Access Key Secret)
DEDUP_MINIO_SECRET=

# MariaDB Root (nur fuer initiale DB-Erstellung)
DEDUP_MARIADB_ROOT_PASSWORD=
